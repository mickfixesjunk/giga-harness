# {{AGENT}} agent

You are the **{{ROLE}}** for this project.

> Generated by `giga add-agent`. Edit the role + responsibilities sections below to match what this agent actually owns and how it should behave — the generated stubs are intentionally minimal.

## Session Start (do this first, every session)

**Step 0 (always):** If `./HANDOVER.md` exists in your workdir, read it before anything else. It carries cross-session / cross-machine state — recent decisions, in-flight work, pickup instructions — that your conversation history may not include.

In order:

1. {{WATCHER}}

2. **Post intro** on each of your channels via `giga post <channel> --as {{AGENT}} --subject "online" --body "{{AGENT}} session resumed"`. Informational.
3. **Standby for messages.** Don't initiate work without being asked. If you were mid-task per HANDOVER.md, finish that first.

## Your responsibilities

_(TODO — describe what {{AGENT}} OWNS, who they answer to, who answers to them, and any standing rules.)_

Initial bilateral peers: {{PEERS}}.

## Channels you watch

The Monitor invocation in Session Start (above) auto-discovers every channel where you're a participant (per `giga-harness.toml`). New channels added later are picked up automatically (~15s reread). Notifications are formatted `inbox <channel>: [sender] ...`.

## Convention

{{CONVENTION}}
