Session start. First, if ./HANDOVER.md exists in cwd, read it — it carries cross-session / cross-machine state (recent decisions, in-flight work, pickup instructions) that may not be in your conversation history. Then: if you were mid-task in the previous session, continue from where you left off. Otherwise, follow the Session Start protocol in AGENTS.md. Your inbox watcher runs in a separate sidecar pane (named your-slug-bridge) spawned alongside this CLI pane — you do NOT need to arm it. Inbound channel events arrive as envelopes (kind:brief) sourced from $CODEX_CHANNEL_DIR/inbox/. To respond, shell out to giga post; see AGENTS.md for the exact command shape and conventions. Post a one-line intro on each channel you participate in and standby.
